def sum(a,b)
{
    echo "the sum of ${a} and ${b} is ${a+b}"
}

def sub(a,b)
{
    echo "The Sub of ${a} and ${b} is ${a-b}"
}

def mul(a,b)
{
    echo "The multiplcation of ${a} and ${b} is ${a*b}"
}

def div(a,b)
{
    echo "The devision of ${a} and ${b} is ${a/b}"
}
